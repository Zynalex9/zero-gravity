import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Zero Gravity | Creative Design Agency",
  description: "We craft bold, innovative visual experiences that defy convention and propel brands into new dimensions of creative excellence.",
  keywords: ["design agency", "graphic design", "branding", "web design", "creative agency"],
  openGraph: {
    title: "Zero Gravity | Creative Design Agency",
    description: "Design Without Boundaries. We craft bold, innovative visual experiences.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
